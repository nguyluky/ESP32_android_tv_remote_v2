#include "remote/remotemessage.pb.h"

class RemoteMessageManager {
public:
    RemoteMessageManager();
    ~RemoteMessageManager();
    
    bool createMessageConfig(remote_RemoteMessage *message) {
        message->remote_configure.
        
        return true;
    }
private:
};